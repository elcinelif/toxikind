model.py:
1. Move model training to .py file including assay selection (manually) and hyperparameters (environment variable)
2. Implement proper saving and loading of trained models (path as env + selection)
3. Implement model evaluation (output as dict)

params.py:
1. Dict of assays (take names from notebook)
2. Data and model paths

env:
1. Data path
2. Model path

main.py:
1. Implement wrappers model_load_predict, model_load_evaluate, model_train_save
2. All I/O here (like main vs. processing) or mixed in models?
